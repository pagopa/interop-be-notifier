akka {
  loglevel = "INFO"
}

notifier {
  document-db {
    uri = ${DOCUMENT_DB_URI}
  }
}

interop-commons {
  vault {
    address = "test"
    token = "test"
  }

  jwt {
    public-keys {
      url = "localhost"
    }

    internal-token {
      issuer = "yada"
      subject = "yada"
      audience = "audience"
      duration-seconds = 100
    }
  }
}